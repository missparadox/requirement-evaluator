# Legacy Report Template

This file is retained for compatibility with existing service code that still references `references/report-template.md`.

The shard-only skill workflow does not use this file. Final reports are rendered by `scripts/evaluate_requirements.py aggregate` from validated shard result JSON files.

